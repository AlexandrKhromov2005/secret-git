# Claude Code / OMC local files — never commit
.claude/
CLAUDE.md
CLAUDE.local.md
.omc/

# Build artifacts
/encgit
/dist/
*.test
*.out

# Docker offline image bundles — generated locally, never commit (huge binary)
Docker/*.tar

# Local scratch
/tmp/
